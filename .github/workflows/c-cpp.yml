name: "C++ CI/CD"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  pre-release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: "tests"
      run: make tests #makefile will auto run test cases
    - name: "build" 
      run: make primeNums
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        name: Auto-generated
        tag_name: lastest
        #todo, correctly compile binary and attach it to release
